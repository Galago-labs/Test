# PLAN.MD — «Крипта Забытого Доктора»  
## Версия для JavaScript / TypeScript (браузер, 2D pixel-art, ECS-like architecture)

Этот документ — пошаговая инструкция для разработки небольшой 2D pixel-art игры на **TypeScript** с итоговой сборкой в **JavaScript**.

Игра делается как браузерная версия оригинального плана:  
**аренный dungeon-survival**, одна комната, 5 волн, босс, доктор, дверь, победа.

Главное изменение по сравнению с Godot-планом:

- вместо сцен/узлов Godot используется **собственный лёгкий движок** на TS/JS;
- игровой код строится как **набор компонентов и систем**, максимально близко к ECS;
- движок не знает ничего про игру;
- игра не лезет внутрь движка, а использует только его публичный API.

---

# 0. Концепт игры

## Жанр
Аренный dungeon-survival: игрок находится в одной комнате подземелья и отбивается от волн врагов.

## Сюжет
Чумной доктор заперт в крипте и удерживает магический барьер у фонтана.  
Рыцарь должен продержаться 5 волн нечисти и убить Большого Демона, чтобы доктор смог открыть дверь и оба выбрались наружу.

## Цикл геймплея
- игрок ходит по комнате;
- рубит врагов мечом, атака — свинг по дуге;
- подбирает монеты и зелья;
- между волнами открывает сундук и получает лучшее оружие;
- побеждает финального босса;
- подходит к доктору;
- получает финальный диалог и экран победы.

## Проигрыш
Здоровье игрока упало до нуля.

## Победа
Убит босс `big_demon`, игрок подходит к доктору, дверь открыта, показывается экран победы.

## Почему так
Одна комната без прокрутки камеры и без процедурной генерации = минимум точек отказа.  
При этом есть:
- челлендж;
- прогрессия;
- сюжет;
- босс;
- полный игровой цикл.

## Осознанное ограничение
Мы **НЕ** делаем:
- процедурную генерацию;
- несколько уровней;
- редактор тайлов;
- сложную физику;
- сетевой код;
- сохранение прогресса;
- инвентарь;
- прокачку сверх оружия из сундука.

Вся сцена строится кодом из ASCII-карты.

---

# 1. Принцип объёма

Игра должна быть **небольшой, но законченной**.

Партия длится примерно **5–8 минут**.

Разрешено ровно:
- 8 типов врагов;
- 4 оружия;
- 3 вида пикапов;
- одна комната;
- один NPC-доктор;
- один сундук;
- один босс.

## Правило урезания
Если что-то не получается, резать строго в этом порядке:

1. шипы;
2. реплики доктора;
3. некромант, заменить на `orc_warrior`;
4. мини-босс `ogre`, заменить на 2 `orc_warrior`.

## Нельзя резать
Никогда не вырезать:
- волны;
- босса;
- меню;
- паузу;
- победу/поражение;
- звук.

Без этого игра не считается законченной.

---

# 2. Целевой стек

## Основной язык
**TypeScript**.

## Итоговый рантайм
**JavaScript** в браузере после сборки.

## Рекомендуемый инструмент сборки
**Vite**.

## Рендер
**Canvas 2D**.

Почему Canvas 2D:
- проект маленький;
- разрешение 480×270;
- легко контролировать пиксельную чёткость;
- не нужно тянуть тяжёлый 3D-движок;
- проще сделать собственный движок и ECS-слой.

## Аудио
**WebAudio API**.

Все звуки синтезируются программно, без внешних аудиофайлов.

## Ввод
- клавиатура;
- мышь.

---

# 3. Архитектура: движок и игра

Это обязательное требование плана.

Проект должен быть разделён на два слоя:

## 3.1. `engine/`
Универсальный игровой движок.

Он отвечает за:
- игровой цикл;
- время;
- ввод;
- рендер;
- камеру;
- загрузку ассетов;
- звук;
- ECS;
- физику/коллизии;
- события;
- управление состояниями приложения.

Движок **не содержит**:
- врагов;
- оружия;
- волн;
- доктора;
- сюжета;
- конкретных спрайтов;
- конкретных правил игры.

## 3.2. `game/`
Сама игра.

Она содержит:
- компоненты конкретной игры;
- системы конкретной игры;
- данные врагов;
- данные оружия;
- волны;
- меню;
- HUD;
- фабрику комнаты;
- фабрики сущностей;
- логику победы/поражения.

Игра использует движок только через публичный интерфейс.

---

# 4. Модульная структура, близкая к ECS

## 4.1. Базовая модель

Используем упрощённый, но честный ECS-подход:

- **Entity** — идентификатор сущности;
- **Component** — чистые данные;
- **System** — логика над компонентами;
- **World** — хранилище сущностей и компонентов;
- **Queries** — выборки сущностей по набору компонентов;
- **Events** — события для слабой связности;
- **Commands** — отложенный спавн/уничтожение сущностей.

Это не обязательно должен быть полновесный промышленный ECS, но структура должна быть именно такой по духу.

---

## 4.2. Компоненты игры

Примерный список компонентов:

### Базовые
- `Transform` — позиция, поворот, масштаб;
- `Velocity` — скорость;
- `Sprite` — статичный спрайт;
- `AnimatedSprite` — анимация;
- `Collider` — физический коллайер;
- `Sensor` — сенсорная зона без физического выталкивания;
- `RenderLayer` — слой отрисовки;
- `YSort` — участвовать в Y-сортировке;
- `ZIndex` — порядок отрисовки;
- `FootAnchor` — привязка «ноги в точку»;
- `Flash` — вспышка при уроне;
- `Knockback` — отброс;
- `TimedDeath` — удалить через время;
- `FadeTween` — анимация исчезновения/появления;
- `SpawnEffect` — эффект появления.

### Игрок
- `PlayerTag`;
- `PlayerControl`;
- `Health`;
- `Invulnerability`;
- `SpeedBoost`;
- `WeaponHolder`;
- `AttackInput`;
- `InteractInput`.

### Оружие
- `Weapon`;
- `WeaponAim`;
- `WeaponSwing`;
- `AttackCooldown`;
- `HitboxActive`;
- `AlreadyHitSet`.

### Враги
- `EnemyTag`;
- `EnemyData`;
- `EnemyAI`;
- `RangedAI`;
- `BossAI`;
- `TouchDamage`;
- `TouchCooldown`;
- `EnemyState`;
- `SpawnProtection`.

### Снаряды
- `Projectile`;
- `ProjectileLifetime`;
- `DamageOnHit`.

### Пикапы
- `Pickup`;
- `PickupMagnet`;
- `PickupValue`.

### Сундук
- `Chest`;
- `ChestState`;
- `ChestReward`.

### Шипы
- `SpikeTrap`;
- `SpikeCycle`;
- `SpikeDamage`.

### Доктор/победа
- `DocNPC`;
- `DocSpeech`;
- `VictoryZone`.

### Волны
- `WaveMember`;
- `SpawnQueueItem`.

---

## 4.3. Системы игры

Примерный список систем:

### Ввод и управление
- `InputSystem`;
- `PlayerControlSystem`;
- `InteractSystem`.

### Бой
- `WeaponAimSystem`;
- `WeaponAttackSystem`;
- `WeaponHitSystem`;
- `PlayerDamageSystem`;
- `EnemyDamageSystem`;
- `HitStopSystem`;
- `KnockbackSystem`;
- `FlashSystem`.

### ИИ
- `EnemyAISystem`;
- `RangedAISystem`;
- `BossAISystem`;
- `ProjectileSystem`.

### Физика
- `MovementSystem`;
- `CollisionResolveSystem`;
- `ContactDamageSystem`.

### Объекты
- `PickupMagnetSystem`;
- `PickupCollectSystem`;
- `ChestInteractSystem`;
- `SpikeSystem`;
- `DocSpeechSystem`;
- `VictoryZoneSystem`.

### Волны
- `WaveSystem`;
- `SpawnSystem`;
- `WaveProgressSystem`;
- `BossBarSystem`.

### Визуал
- `AnimationSystem`;
- `YSortRenderSystem`;
- `CameraShakeSystem`;
- `ScreenFlashSystem`;
- `ParticleBurstSystem`.

### UI
- `HUDSyncSystem`;
- `MenuSystem`;
- `PauseSystem`;
- `GameOverSystem`;
- `VictoryScreenSystem`.

---

## 4.4. Порядок обновления

Рекомендуемый порядок:

1. `InputSystem`
2. `StateControlSystem`
3. `PlayerControlSystem`
4. `WeaponAimSystem`
5. `WeaponAttackSystem`
6. `EnemyAISystem`
7. `RangedAISystem`
8. `BossAISystem`
9. `ProjectileSystem`
10. `MovementSystem`
11. `CollisionResolveSystem`
12. `WeaponHitSystem`
13. `ContactDamageSystem`
14. `SpikeSystem`
15. `PickupMagnetSystem`
16. `PickupCollectSystem`
17. `ChestInteractSystem`
18. `WaveSystem`
19. `AnimationSystem`
20. `EffectsSystem`
21. `HUDSyncSystem`
22. `RenderSystem`

---

# 5. Структура проекта

Рекомендуемая структура:

```text
crypt-of-lost-doc-ts/
  index.html
  package.json
  tsconfig.json
  vite.config.ts
  public/
    assets/
      characters/
      enemies/
      environment/
      items/
      ui/
      weapons/
  src/
    engine/
      core/
        GameLoop.ts
        Time.ts
        EventBus.ts
        StateManager.ts
        Random.ts
      ecs/
        World.ts
        Entity.ts
        ComponentStore.ts
        System.ts
        Query.ts
        Commands.ts
      math/
        Vector2.ts
        Rect.ts
        MathUtils.ts
      input/
        InputManager.ts
        ActionMap.ts
      assets/
        AssetManager.ts
        SpriteFrames.ts
        Manifest.ts
      render/
        Renderer2D.ts
        Camera2D.ts
        Sprite.ts
        AnimatedSprite.ts
        RenderQueue.ts
        Layers.ts
      physics/
        CollisionWorld.ts
        Body.ts
        CollisionLayers.ts
        Move.ts
      audio/
        SynthAudio.ts
        SfxPool.ts
      ui/
        DomOverlay.ts
      index.ts
    game/
      main.ts
      config.ts
      strings.ts
      manifest.ts
      art.ts
      roomBuilder.ts
      session/
        GameSession.ts
      data/
        enemies.ts
        weapons.ts
        waves.ts
        docLines.ts
        chestRewards.ts
      components/
        index.ts
        player.ts
        enemy.ts
        weapon.ts
        projectile.ts
        pickup.ts
        chest.ts
        spikes.ts
        doc.ts
        wave.ts
        effects.ts
      systems/
        input.ts
        player.ts
        weapon.ts
        enemy.ts
        projectile.ts
        pickups.ts
        chest.ts
        spikes.ts
        waves.ts
        doc.ts
        hud.ts
        pause.ts
        menus.ts
        effects.ts
        camera.ts
      factories/
        player.ts
        enemy.ts
        projectile.ts
        pickup.ts
        chest.ts
        spikes.ts
        doc.ts
        room.ts
      ui/
        hud.ts
        menus.ts
      states/
        MenuState.ts
        PlayState.ts
        PauseOverlay.ts
        GameOverOverlay.ts
        VictoryOverlay.ts
```

---

# 6. Правило разделения слоёв

## Разрешено
- `game` импортирует `engine`;
- `game` использует публичный API движка;
- `game` определяет компоненты, системы и данные.

## Запрещено
- `engine` импортирует `game`;
- `engine` знает про `big_demon`, `knight`, `waves`, `doctor`;
- игровая логика пишется внутри движка;
- движок содержит конкретные пути к ассетам игры.

## Рекомендация
Использовать алиасы:

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@engine/*": ["src/engine/*"],
      "@game/*": ["src/game/*"]
    }
  }
}
```

И запретить в линтере импорты из `game` внутрь `engine`.

---

# 7. Технический фундамент

## 7.1. Разрешение
Логический размер сцены:

```text
480 × 270
```

Отображение:

```text
960 × 540
```

Масштаб строго целый: `×2`.

## 7.2. Пиксельная чёткость
Обязательно:

- `ctx.imageSmoothingEnabled = false`;
- CSS `image-rendering: pixelated`;
- никакого дробного масштабирования спрайтов;
- лучше округлять позиции при отрисовке:
  ```ts
  Math.round(x), Math.round(y)
  ```

## 7.3. Игровой цикл
Использовать:

```ts
requestAnimationFrame
```

Рекомендуется:
- фиксированный физический шаг `1/60`;
- аккумулятор времени;
- ограничение `delta`, чтобы не было скачков после потери фокуса.

## 7.4. Время
В движке должны быть:

- `timeScale` — для hit-stop и slow-motion;
- `paused` — для паузы;
- обычные таймеры;
- «реальные» таймеры, игнорирующие `timeScale`, если нужно.

## 7.5. Состояния приложения
Глобальные состояния:

```ts
enum AppState {
  BOOT,
  MENU,
  GAME,
}
```

Внутри игры:

```ts
enum GameState {
  MENU,
  PLAYING,
  PAUSED,
  DIALOG,
  GAME_OVER,
  VICTORY,
}
```

---

# 8. Ввод

## 8.1. Действия
Сделать систему действий, аналогичную Godot InputMap.

Действия:
- `move_left`
- `move_right`
- `move_up`
- `move_down`
- `attack`
- `interact`
- `pause`

## 8.2. Привязка
- движение: `WASD` и стрелки;
- атака: левая кнопка мыши и `Space`;
- взаимодействие: `E`;
- пауза: `Escape`.

## 8.3. Требования
- использовать `KeyboardEvent.code`;
- предотвращать скролл страницы для стрелок и пробела;
- координаты мыши пересчитывать из CSS-масштаба в логические координаты 480×270.

---

# 9. Координаты и комната

## 9.1. Размер комнаты
ASCII-карта: **28 × 15** тайлов.

Тайл: **16 × 16**.

Размер комнаты в пикселях:

```text
448 × 240
```

## 9.2. Смещение мира
Чтобы комната была видна целиком и сверху осталось место под HUD:

```ts
const WORLD_OFFSET = new Vector2(16, 24);
```

## 9.3. Внутренняя зона пола
В мировых координатах:

```ts
new Rect(
  WORLD_OFFSET.x + 16,
  WORLD_OFFSET.y + 16,
  416,
  208
)
```

То есть:

```ts
x = 32
y = 40
w = 416
h = 208
```

---

# 10. ASCII-карта комнаты

Используем ровно ту же карту:

```text
############D###############
#..........................#
#.1......................2.#
#....C..............C......#
#..........................#
#......S..........S........#
#............P.............#
#..........................#
#......S..........S........#
#...C..................C...#
#..........................#
#.3......................4.#
#.....K....................#
#.........X.....O..........#
############################
```

## Легенда
- `#` — стена-периметр;
- `.` — пол;
- `S` — шипы;
- `C` — ящик;
- `K` — череп-декор;
- `D` — дверь в центре верхней стены;
- `1..4` — точки спавна врагов;
- `P` — старт игрока;
- `X` — сундук;
- `O` — позиция доктора.

Фонтан `F` не в карте: он рисуется поверх верхней стены.

---

# 11. Ассеты

Все ассеты берутся из оригинальной папки `plan/assets` и копируются в `public/assets`.

После копирования должно быть:

```text
public/assets/characters/
public/assets/enemies/
public/assets/environment/
public/assets/items/
public/assets/ui/
public/assets/weapons/
```

---

## 11.1. Герои

Папка:

```text
assets/characters/
```

Используемые:

| Персонаж | Префикс файлов | Размер | Анимации |
|---|---|---:|---|
| Рыцарь (игрок) | `knight_m_` | 16×28 | idle ×4, run ×4, hit ×1 |
| Доктор (NPC) | `doc_` | 16×23 | idle ×4, run ×4 |

Примеры:
```text
characters/knight_m_idle_anim_f0.png
characters/knight_m_run_anim_f0.png
characters/knight_m_hit_anim_f0.png
characters/doc_idle_anim_f0.png
```

---

## 11.2. Враги

Папка:

```text
assets/enemies/
```

Используем ровно 8 типов:

| Враг | Префикс | Размер | Кадры |
|---|---|---:|---|
| `tiny_zombie` | `tiny_zombie_` | 16×16 | idle ×4 + run ×4 |
| `goblin` | `goblin_` | 16×16 | idle ×4 + run ×4 |
| `skelet` | `skelet_` | 16×16 | idle ×4 + run ×4 |
| `imp` | `imp_` | 16×16 | idle ×4 + run ×4 |
| `orc_warrior` | `orc_warrior_` | 16×23 | idle ×4 + run ×4 |
| `necromancer` | `necromancer_` | 16×23 | только `necromancer_anim_f0..f3` |
| `ogre` | `ogre_` | 32×36 | idle ×4 + run ×4 |
| `big_demon` | `big_demon_` | 32×36 | idle ×4 + run ×4 |

## Ловушки в именах
- Скелет называется `skelet_`, а не `skeleton_`.
- У `necromancer` файлы:
  ```text
  necromancer_anim_f0..f3
  ```
  без слов `idle` и `run`.
- У обычного `zombie` нет `f0`, поэтому обычный `zombie` не используется.
- `goblin`, `skelet`, `imp` — реально 16×16.
- Остальные враги не используются.

---

## 11.3. Окружение

Папка:

```text
assets/environment/
```

## Полы
```text
floor_1.png ... floor_8.png
floor_spikes_anim_f0..f3
```

## Стены
```text
wall_mid.png
wall_top_mid.png
wall_left.png
wall_right.png
wall_edge_*
wall_outer_*
wall_hole_1/2.png
wall_goo.png
wall_goo_base.png
```

## Декор
```text
wall_banner_blue.png
wall_banner_green.png
wall_banner_red.png
wall_banner_yellow.png
skull.png
crate.png
column.png
column_wall.png
```

## Фонтан
```text
wall_fountain_top_1/2/3.png
wall_fountain_mid_blue_anim_f0..f2
wall_fountain_basin_blue_anim_f0..f2
```

## Дверь
```text
doors_leaf_closed.png
doors_leaf_open.png
doors_frame_left.png
doors_frame_right.png
doors_frame_top.png
```

## Механизмы
```text
button_red/blue_up/down.png
lever_left/right.png
```

---

## 11.4. Предметы

Папка:

```text
assets/items/
```

## Монета
```text
coin_anim_f0..f3
```

## Зелья
```text
flask_red.png
flask_blue.png
flask_green.png
flask_yellow.png
flask_big_red.png
flask_big_blue.png
flask_big_green.png
flask_big_yellow.png
```

## Сундуки
```text
chest_full_open_anim_f0..f2
chest_empty_open_anim_f0..f2
chest_mimic_open_anim_f0..f2
```

---

## 11.5. UI

Папка:

```text
assets/ui/
```

Только три файла:

```text
ui_heart_full.png
ui_heart_half.png
ui_heart_empty.png
```

Размер: 13×12.

---

## 11.6. Оружие

Папка:

```text
assets/weapons/
```

Используем ровно 5 файлов:

```text
weapon_rusty_sword.png
weapon_regular_sword.png
weapon_knight_sword.png
weapon_red_gem_sword.png
weapon_knife.png
```

---

# 12. Данные игры

## 12.1. Оружие

Ровно 4 оружия игрока:

| id | текстура | урон | радиус | дуга | кулдаун |
|---|---|---:|---:|---:|---:|
| `rusty` | `weapon_rusty_sword.png` | 1 | 16 | 100° | 0.45 |
| `regular` | `weapon_regular_sword.png` | 2 | 16 | 100° | 0.45 |
| `knight` | `weapon_knight_sword.png` | 3 | 20 | 130° | 0.40 |
| `gem` | `weapon_red_gem_sword.png` | 4 | 20 | 130° | 0.35 |

## Прогрессия
Старт:
- `rusty`

После волн:
- 1 → `regular`
- 2 → `knight`
- 3 → `gem`

Перед боссом, после волны 4:
- сундук выдаёт не оружие, а полное лечение через `flask_big_red`.

---

## 12.2. Враги

Таблица врагов в `game/data/enemies.ts`:

```ts
export const ENEMIES = {
  tiny_zombie: {
    w: 16, h: 16, hp: 2, speed: 55, dmg: 1, coins: 1,
    prefix: "enemies/tiny_zombie_", twoAnims: true,
  },
  goblin: {
    w: 16, h: 16, hp: 3, speed: 70, dmg: 1, coins: 1,
    prefix: "enemies/goblin_", twoAnims: true,
  },
  skelet: {
    w: 16, h: 16, hp: 4, speed: 50, dmg: 1, coins: 2,
    prefix: "enemies/skelet_", twoAnims: true,
  },
  imp: {
    w: 16, h: 16, hp: 2, speed: 95, dmg: 1, coins: 2,
    prefix: "enemies/imp_", twoAnims: true,
  },
  orc_warrior: {
    w: 16, h: 23, hp: 8, speed: 55, dmg: 2, coins: 3,
    prefix: "enemies/orc_warrior_", twoAnims: true,
  },
  necromancer: {
    w: 16, h: 23, hp: 5, speed: 40, dmg: 1, coins: 4,
    prefix: "enemies/necromancer_", twoAnims: false, ranged: true,
  },
  ogre: {
    w: 32, h: 36, hp: 25, speed: 40, dmg: 2, coins: 10,
    prefix: "enemies/ogre_", twoAnims: true,
  },
  big_demon: {
    w: 32, h: 36, hp: 60, speed: 45, dmg: 2, coins: 30,
    prefix: "enemies/big_demon_", twoAnims: true, boss: true,
  },
} as const;
```

## Правило анимаций
Если `twoAnims === true`:
```ts
idle = prefix + "idle_anim_"
run  = prefix + "run_anim_"
```

Если `twoAnims === false`:
```ts
idle = prefix + "anim_"
run  = prefix + "anim_"
```

---

## 12.3. Волны

```ts
export const WAVES = [
  ["tiny_zombie", "tiny_zombie", "tiny_zombie", "goblin", "goblin"],
  ["goblin", "goblin", "skelet", "skelet", "imp", "imp", "tiny_zombie", "tiny_zombie"],
  ["orc_warrior", "orc_warrior", "necromancer", "imp", "imp", "skelet", "skelet"],
  ["ogre", "orc_warrior", "necromancer", "necromancer", "imp", "imp"],
  ["big_demon", "imp", "imp"],
] as const;

export const BETWEEN_WAVES_DELAY = 4.0;
```

---

## 12.4. Пикапы

Ровно 3 типа пикапов:

1. `coin`
2. `flask_red`
3. `flask_blue`

## Эффекты
- `coin`: +1 монета;
- `flask_red`: лечит 2 единицы здоровья;
- `flask_blue`: даёт `speed_boost = 4.0` секунды.

## Важно
`flask_big_red` — **не пикап**.  
Это награда из сундука перед боссом: показываем спрайт и сразу лечим игрока до полного.

---

## 12.5. Награды сундука

Порядок открытий:

1. после волны 1 → `regular`
2. после волны 2 → `knight`
3. после волны 3 → `gem`
4. после волны 4 → `flask_big_red`

Баннеры:
- `Новое оружие: …!`
- `Ты готов. Он идёт.`

---

## 12.6. Реплики доктора

Старт:
> Рыцарь! Барьер держится, пока я у фонтана. Защити меня — и я открою дверь!

После волны 1:
> Отлично! Возьми оружие из сундука — оно тебе понадобится.

После волны 2:
> Они идут из глубин… Держись!

После волны 3:
> Чувствую тяжёлую поступь… Огр близко!

После волны 4:
> Это он… Повелитель крипты! Убей его — и мы свободны!

После босса:
> Путь открыт! Подойди ко мне, уходим!

---

# 13. Слои коллизий

Используем битовые слои.

| Слой | Что это |
|---|---|
| 1 | стены / препятствия |
| 2 | игрок |
| 3 | враги |
| 4 | хитбокс оружия игрока |
| 5 | вражеские снаряды |
| 6 | подбираемые предметы |

## Настройки
### Игрок
```ts
layer = 2
mask  = 1 | 3
```

### Враг
```ts
layer = 3
mask  = 1 | 2 | 3
```

### Оружие
```ts
layer = 4
mask  = 3
```

### Снаряд
```ts
layer = 5
mask  = 1 | 2
```

### Пикап
```ts
layer = 6
mask  = 2
```

---

# 14. Движковые требования

Ниже — что должно быть реализовано в `engine/`.

---

## 14.1. GameLoop
Должен:
- запускаться через `requestAnimationFrame`;
- поддерживать `update(dt)` и `render(alpha?)`;
- учитывать `paused`;
- учитывать `timeScale`;
- не ломаться при потере фокуса.

---

## 14.2. EventBus
Простая шина событий.

Примеры игровых событий:
- `player:damaged`
- `player:died`
- `enemy:damaged`
- `enemy:died`
- `wave:started`
- `wave:cleared`
- `boss:killed`
- `chest:opened`
- `pickup:collected`
- `ui:banner`

---

## 14.3. World / ECS
Требования:
- сущности — числовые id;
- компоненты — в `Map` или typed-хранилищах;
- системы — классы или функции с `update(world, dt)`;
- безопасное удаление сущностей через очередь команд;
- нельзя мутировать набор сущностей во время итерации напрямую.

---

## 14.4. Renderer2D
Должен уметь:
- рисовать `Texture2D`;
- рисовать кадры анимации;
- поддерживать `camera offset`;
- поддерживать слои;
- поддерживать Y-сортировку;
- рисовать текст;
- рисовать прямоугольники для отладки;
- очищать кадр;
- отключать сглаживание.

---

## 14.5. AssetManager
Должен:
- грузить изображения по манифесту;
- возвращать `HTMLImageElement` или `ImageBitmap`;
- проверять ошибки загрузки;
- не давать использовать непроверенные пути.

Рекомендуется:
- заранее сгенерированный `manifest.ts`;
- проверка в консоли, что все файлы загружены.

---

## 14.6. InputManager
Должен:
- отслеживать нажатые клавиши;
- поддерживать действия;
- отслеживать позицию мыши;
- поддерживать `justPressed` и `held`;
- корректно работать после паузы и перезапуска.

---

## 14.7. Physics / Collision
Минимально достаточный набор:
- статические AABB;
- динамические AABB;
- сенсорные зоны;
- маска/слой;
- простое выталкивание;
- движение с коллизиями, аналог `move_and_slide`.

Можно сделать без полноценного физического движка.

---

## 14.8. Audio
Нужен программный синтез звуков через `AudioBuffer`.

Важно:
- запуск `AudioContext` только после пользовательского действия;
- пул из нескольких источников;
- громкость не выше `0.5`.

---

# 15. Игровые требования

Ниже — что должно быть реализовано в `game/`.

---

# 16. Комната

## 16.1. Построение
Комната строится кодом из ASCII-карты.

Создаются:
- слой пола;
- слой стен;
- статические коллайдеры;
- декор;
- точки спавна;
- возвращаемые данные для главных объектов.

## 16.2. Возвращаемые данные
`roomBuilder` должен вернуть:

```ts
{
  playerPos: Vector2,
  spawnPoints: Vector2[],
  chestPos: Vector2,
  docPos: Vector2,
  spikeCells: Vector2[],
  crateCells: Vector2[],
  doorSpriteRef: Entity,
}
```

---

## 16.3. Пол
Для каждой клетки без `#`:
- 80% — `floor_1.png`;
- иначе случайный из `floor_2..floor_8`.

Рекомендация:
- статичный пол один раз отрендерить в offscreen-canvas.

---

## 16.4. Стены
Упрощённая схема:
- верхний ряд: `wall_mid.png`;
- над верхним рядом: `wall_top_mid.png` со смещением `y - 16`;
- нижний ряд: `wall_top_mid.png`;
- левый и правый столбцы: `wall_mid.png`.

---

## 16.5. Декор
На верхней стене:
- `wall_banner_red.png` на col 5;
- `wall_banner_blue.png` на col 22;
- фонтан на col 9:
  - `wall_fountain_mid_blue_anim_f0..f2` в row 0;
  - `wall_fountain_basin_blue_anim_f0..f2` в row 1;
- `wall_goo.png` на col 17;
- `wall_goo_base.png` под ним в row 1.

---

## 16.6. Дверь
На клетках `col 12-13`, `row 0`:
- `doors_leaf_closed.png`;
- сохранить ссылку;
- в финале заменить на `doors_leaf_open.png`.

---

## 16.7. Ящики
Ящик `C`:
- статическое препятствие;
- спрайт `crate.png`;
- коллизия примерно 14×12;
- не разрушается.

---

## 16.8. Череп
Череп `K`:
- только декор;
- без коллизии.

---

# 17. Игрок

## 17.1. Параметры
```ts
SPEED = 90
MAX_HP = 6
```

`MAX_HP = 6` означает 3 сердца, где 1 единица = половина сердца.

## 17.2. Компоненты
Игрок имеет:
- `Transform`
- `Velocity`
- `AnimatedSprite`
- `Collider`
- `PlayerTag`
- `PlayerControl`
- `Health`
- `Invulnerability`
- `SpeedBoost`
- `WeaponHolder`

## 17.3. Анимации
Использовать:
```text
characters/knight_m_idle_anim_f0..f3
characters/knight_m_run_anim_f0..f3
```

Размер спрайта:
```text
16 × 28
```

Ноги должны стоять на позиции сущности.

## 17.4. Движение
Если состояние игры не `PLAYING`:
- не двигать игрока;
- не обрабатывать атаку.

Иначе:
- читать направление из ввода;
- применять скорость;
- учитывать `speed_boost`;
- выполнять движение с коллизиями.

## 17.5. Анимация
- `run`, если скорость > 5;
- иначе `idle`;
- флип по горизонтали по знаку `velocity.x`.

## 17.6. Урон
`takeDamage(amount)`:
- если есть неуязвимость — игнорировать;
- уменьшить `hp`;
- включить `invuln = 0.9`;
- проиграть звук `hurt`;
- белая вспышка;
- отброс;
- если `hp <= 0` — смерть.

## 17.7. Неуязвимость
Пока активна:
- мигание спрайта;
- повторный урон не проходит.

## 17.8. Лечение
`heal(amount)`:
- не выше `MAX_HP`;
- зелёная вспышка.

## 17.9. Смерть
При смерти:
- состояние `GAME_OVER`;
- твин: покраснение, поворот, уменьшение масштаба;
- через 1 секунду показать экран поражения.

---

# 18. Оружие

## 18.1. Общая идея
Атака делается свингом спрайта оружия по дуге.

## 18.2. Структура
Оружие — отдельная сущность или дочерний объект игрока:
- позиция примерно на уровне рук;
- поворот направлен на курсор;
- спрайт оружия смещён вдоль оси;
- хитбокс активен только во время атаки.

## 18.3. Направление
Текстуры оружия нарисованы остриём вверх.

Поэтому:
- направление задаётся поворотом;
- дополнительно добавляется коррекция `+ PI/2`.

## 18.4. Поведение вне атаки
Каждый кадр:
- оружие поворачивается к мыши;
- рисуется перед игроком, если мышь ниже;
- рисуется за игроком, если мышь выше.

## 18.5. Атака
При атаке:
- если кулдаун прошёл;
- запускается свинг;
- хитбокс включается;
- список уже поражённых врагов сбрасывается;
- проигрывается звук `swing`.

## 18.6. Свинг
Анимация поворота:
- от `aimAngle - arc/2`
- до `aimAngle + arc/2`
- длительность примерно `0.12`

После:
- хитбокс выключается;
- кулдаун начинает тикать.

## 18.7. Попадание
Во время свинга:
- проверяется пересечение хитбокса с врагами;
- каждый враг может быть поражён только один раз за свинг;
- вызывается `enemy.takeDamage(damage, direction)`.

---

# 19. Враги

## 19.1. Общий подход
Все враги — сущности с:
- `EnemyTag`
- `EnemyData`
- `Health`
- `Transform`
- `Velocity`
- `AnimatedSprite`
- `Collider`
- `TouchDamage`

## 19.2. Спавн-эффект
При появлении:
- прозрачность 0;
- масштаб 0.3;
- анимация появления 0.4 сек;
- первые 0.5 сек враг не двигается и не наносит урон.

## 19.3. Обычный ИИ
Каждый кадр:
- направление к игроку;
- скорость из таблицы;
- движение с коллизиями;
- анимация `run`;
- флип по направлению.

## 19.4. Некромант
Поведение:
- если дистанция > 90 — идти к игроку;
- если дистанция < 60 — отходить;
- каждые 2 секунды стрелять снарядом;
- звук `cast`.

Если упёрся в стену:
- допустимо стоять и стрелять.

## 19.5. Босс
`big_demon`:
- ведёт себя как обычный враг;
- каждые 4 секунды выпускает веер из 8 снарядов по кругу;
- при `hp < 50%` впадает в ярость:
  - скорость ×1.3;
  - веер каждые 2.5 секунды;
  - слегка красная подсветка.

Без призыва миньонов.

## 19.6. Контактный урон
Если:
- враг уже заспавнился;
- дистанция до игрока меньше `(w/2 + 8)`;
- кулдаун контакта истёк;

то:
- игрок получает `dmg`;
- `touchCooldown = 0.8`.

## 19.7. Получение урона врагом
При попадании:
- уменьшить `hp`;
- звук `hit`;
- белая вспышка;
- отброс;
- короткий hit-stop;
- если `hp <= 0` — смерть.

## 19.8. Смерть врага
При смерти:
- отключить ИИ и коллизию;
- проиграть твин смерти:
  - поворот;
  - покраснение/прозрачность;
  - увеличение масштаба;
- выпустить частицы;
- для крупных врагов оставить декал `skull.png`;
- дропнуть монеты и, возможно, зелья;
- отправить событие `enemy:died`;
- удалить сущность после эффекта.

## 19.9. Дроп
Из врага выпадают:
- монеты в количестве из таблицы;
- 15% шанс `flask_red`;
- 8% шанс `flask_blue`.

---

# 20. Снаряды

## 20.1. Внешний вид
Использовать:
```text
weapons/weapon_knife.png
```

Сделать магическую подсветку:
- фиолетово-красный `modulate`.

## 20.2. Параметры
- скорость 120;
- время жизни 3 секунды;
- урон 1;
- летит в игрока или по кругу у босса.

## 20.3. Столкновения
При попадании:
- в игрока — урон и удаление;
- в стену — удаление;
- по таймеру — удаление.

---

# 21. Пикапы

## 21.1. Типы
Только три:
- `coin`
- `flask_red`
- `flask_blue`

## 21.2. Спавн
При выпадении:
- разлёт от точки смерти;
- небольшая анимация появления.

## 21.3. Магнит
Для монет:
- если дистанция до игрока < 24 — лететь к игроку со скоростью 140.

## 21.4. Подбор
При касании игрока:
- монета: `coins += 1`, звук `coin`;
- красная фляга: `heal(2)`, звук `potion`;
- синяя фляга: `speedBoost = 4.0`, звук `potion`;
- затем эффект исчезновения и удаление.

---

# 22. Сундук

## 22.1. Расположение
Позиция `X` из карты.

## 22.2. Состояния
- закрыт;
- открывается;
- открыт;
- ожидание следующей волны;
- снова активен перед следующим перерывом.

## 22.3. Открытие
Вариант по плану:
- открывается по клавише `E`;
- если игрок рядом, над сундуком показывается подсказка `E`.

## 22.4. Анимация
Использовать:
```text
items/chest_full_open_anim_f0.png
items/chest_full_open_anim_f1.png
items/chest_full_open_anim_f2.png
```

## 22.5. Награды
Выдаются по порядку:
1. `regular`
2. `knight`
3. `gem`
4. `flask_big_red`

## 22.6. Логика волны
После зачистки волны:
- сундук перезаряжается;
- следующая волна стартует:
  - после открытия сундука + 2 секунды;
  - либо автоматически через 10 секунд, если игрок игнорирует сундук.

---

# 23. Волны

## 23.1. Состояния боя
- `wave_idle` — перерыв;
- `wave_active` — волна идёт.

## 23.2. Старт игры
После нажатия «Играть»:
- показать сюжетный баннер;
- через 3 секунды начать волну 1.

## 23.3. Старт волны
При старте:
- установить номер волны;
- обновить HUD;
- показать баннер:
  - для 1–4: `Волна n/5`;
  - для 5: `БОСС: ПОВЕЛИТЕЛЬ КРИПТЫ`;
- спавнить врагов по одному каждые 0.7 секунды;
- точка спавна — случайная из 4.

## 23.4. Прогресс волны
Вести количество живых врагов.

Когда живых не осталось:
- `Волна зачищена!`;
- звук `wave`;
- перезарядить сундук;
- показать реплику доктора;
- перейти в перерыв.

## 23.5. Босс
Для босса:
- сверху отображается полоса HP;
- после смерти — особая постановка.

---

# 24. Доктор

## 24.1. Расположение
Позиция `O` из карты.

## 24.2. Анимация
Использовать:
```text
characters/doc_idle_anim_f0..f3
```

## 24.3. Роль
- стоит всю игру;
- враги его игнорируют;
- служит сюжетной точкой и зоной победы.

## 24.4. Реплики
Метод `say(text)`:
- показывает текст над головой;
- текст живёт около 3 секунд;
- плавное появление и исчезновение.

## 24.5. Победа
После смерти босса:
- дверь открывается;
- доктор говорит финальную реплику;
- вокруг доктора создаётся зона победы;
- если игрок подошёл ближе чем на 20 пикселей — `VICTORY`.

---

# 25. HUD

HUD должен быть отдельным игровым модулем.

## 25.1. Сердца
Слева сверху:
- 3 сердца;
- состояния:
  - `full`;
  - `half`;
  - `empty`.

Логика:
- `hp >= i*2 + 2` → full;
- `hp == i*2 + 1` → half;
- иначе empty.

Использовать:
```text
ui/ui_heart_full.png
ui/ui_heart_half.png
ui/ui_heart_empty.png
```

Масштаб: ×2.

## 25.2. Монеты
Справа сверху:
- иконка `coin_anim_f0.png`;
- число монет;
- лёгкий пульс при подборе.

## 25.3. Волна
По центру сверху:
- `Волна 1/5`.

## 25.4. Баннеры
Большие тексты по центру:
- старт сюжета;
- волна;
- босс;
- волна зачищена;
- новое оружие;
- финальные фразы.

## 25.5. Полоса здоровья босса
Сверху:
- серый фон;
- красная полоса по текущему `hp / maxHp`.

---

# 26. Меню, пауза, победа, поражение

Реализовать как отдельные оверлеи.

Можно использовать:
- canvas-фон;
- DOM-кнопки поверх.

---

## 26.1. Главное меню
Заголовок:
> КРИПТА ЗАБЫТОГО ДОКТОРА

Подзаголовок:
> Доктор заперт в крипте. Продержись 5 волн и убей Повелителя крипты.

Управление:
> WASD — движение, мышь/ЛКМ — атака, E — сундук, Esc — пауза

Кнопки:
- Играть
- Выход

Декор:
- рыцарь слева, `idle`, масштаб ×3;
- `big_demon` справа, `idle`, масштаб ×3.

## 26.2. Пауза
Заголовок:
> ПАУЗА

Кнопки:
- Продолжить
- В главное меню
- Выход

## 26.3. Поражение
Заголовок:
> ТЫ ПАЛ В КРИПТЕ…

Статистика:
- Волн пройдено: X
- Убийств: Y
- Монет: Z

Кнопки:
- Ещё раз
- В меню

## 26.4. Победа
Заголовок:
> СВОБОДА! Доктор спасён!

Статистика:
- Волн пройдено
- Убийств
- Монет
- Итоговый счёт: `монеты * 10 + убийства * 5`

Кнопки:
- Ещё раз
- В меню

## 26.5. Пауза в браузере
Важно:
- кнопки должны быть кликабельны во время паузы;
- игровой мир должен быть остановлен;
- оверлей должен обрабатываться всегда.

## 26.6. Кнопка «Выход»
Так как браузер не всегда позволяет закрыть вкладку:
- попытаться вызвать `window.close()`;
- если не получилось, показать сообщение:
  > Закройте вкладку вручную

---

# 27. Звук

Все звуки синтезируются программно.

## 27.1. Список звуков
Нужно ровно 11 звуков:

- `swing`
- `hit`
- `hurt`
- `die`
- `coin`
- `potion`
- `chest`
- `cast`
- `wave`
- `boom`
- `click`

## 27.2. Реализация
В движке сделать:
- генерацию PCM-буферов;
- пул из нескольких источников;
- метод `play(name)`.

## 27.3. Где вызывать
- атака: `swing`;
- попадание по врагу: `hit`;
- урон игроку: `hurt`;
- смерть врага: `die`;
- монета: `coin`;
- зелье: `potion`;
- сундук: `chest`;
- выстрел некроманта/босса: `cast`;
- конец волны: `wave`;
- смерть босса: `boom`;
- кнопки: `click`.

## 27.4. Ограничения
- громкость ≤ 0.5;
- не запускать звук, если он уже играет слишком часто;
- `AudioContext` стартовать по первому клику.

---

# 28. Камера и тряска

## 28.1. Камера
Камера не скроллит мир, но существует для:
- тряски;
- смещения;
- возможных будущих эффектов.

Базовая позиция:
```ts
Vector2(240, 135)
```

## 28.2. Тряска
Поле `shake`.

Каждый кадр:
```ts
camera.offset = randomDir * shake;
shake = moveToward(shake, 0, 20 * dt);
```

## 28.3. Значения тряски
- удар по врагу: +1.5;
- урон игроку: +4;
- смерть огра/босса: +8;
- взрыв/босс-смерть: +6.

---

# 29. Шипы

## 29.1. Анимация
Использовать:
```text
environment/floor_spikes_anim_f0..f3
```

## 29.2. Цикл
- 1.6 секунды убраны (`f0`);
- выдвижение `f1 → f2 → f3` по 0.08 секунды;
- 1.0 секунда полностью выдвинуты (`f3`);
- обратно.

## 29.3. Сдвиг фазы
Каждый экземпляр шипов получает случайный стартовый сдвиг.

## 29.4. Урон
Когда шипы полностью выдвинуты:
- игрок получает 1 урон при дистанции < 8;
- враги получают 1 урон при дистанции < 8;
- для врагов не чаще раза в 0.5 секунды.

## 29.5. Отрисовка
Шипы рисуются на слое пола, ниже персонажей.

---

# 30. Эффекты

Обязательные эффекты:

## 30.1. Вспышка урона
Кратковременно сделать спрайт белым/красным.

## 30.2. Отброс
Небольшое смещение от источника урона.

## 30.3. Hit-stop
При попадании:
- `timeScale = 0.05`;
- вернуть через ~0.04 секунды реального времени.

## 30.4. Частицы
Использовать простой встроенный партикль-модуль:
- искры при попадании;
- кровь/пыль при смерти;
- только короткие импульсы;
- обязательная очистка после завершения.

## 30.5. Смерть босса
- `timeScale = 0.2` на 0.5 секунды;
- белая вспышка экрана;
- звук `boom`;
- сильная тряска.

## 30.6. Монеты
- пульс счётчика монет при подборе.

---

# 31. Этапы разработки

Работать строго по порядку:

```text
M0 → M8
```

После каждого этапа запускать игру и проверку.  
Не переходить к следующему этапу, пока текущий не работает.

---

# M0. Каркас проекта

## Цель
Получить пустой, но работающий TypeScript/Vite проект с движком и игрой.

## Шаги
1. Создать проект:
   ```bash
   npm create vite@latest crypt-of-lost-doc-ts -- --template vanilla-ts
   cd crypt-of-lost-doc-ts
   npm install
   ```

2. Создать структуру:
   ```text
   src/engine/
   src/game/
   public/assets/
   ```

3. Скопировать ассеты:
   ```bash
   cp -R ../plan/assets public/assets
   ```
   или вручную скопировать папку `assets`.

4. Настроить `tsconfig.json`:
   - strict;
   - paths `@engine/*`, `@game/*`.

5. Настроить `vite.config.ts`:
   - алиасы;
   - dev-server.

6. Создать `index.html`:
   - canvas 480×270;
   - контейнер для UI;
   - CSS pixelated.

7. Создать базовые модули движка:
   - `GameLoop`;
   - `Time`;
   - `EventBus`;
   - `AssetManager`;
   - `Renderer2D`;
   - `InputManager`;
   - `World`;
   - `StateManager`.

8. Создать `game/main.ts`:
   - инициализация движка;
   - вывод `boot ok`.

9. Создать `game/config.ts`:
   - размеры;
   - мировые константы;
   - слои коллизий.

10. Создать `game/manifest.ts`:
   - список разрешённых ассетов.

## Проверка M0
- `npm run dev` запускает проект;
- в браузере виден чёрный канвас 960×540;
- в консоли `boot ok`;
- нет ошибок TypeScript;
- нет ошибок в консоли браузера.

---

# M1. Хелперы графики + комната из ASCII-карты

## Цель
Построить комнату из карты.

## Шаги
1. Реализовать в движке:
   - загрузку текстур;
   - `SpriteFrames`;
   - рендер спрайтов;
   - отрисовку без сглаживания.

2. Создать `game/art.ts`:
   - хелперы для путей;
   - сборка анимаций;
   - foot-anchor спрайтов.

3. Реализовать `game/roomBuilder.ts`:
   - парсинг карты;
   - генерация пола;
   - генерация стен;
   - декор;
   - дверь;
   - фонтан;
   - ящики;
   - статические коллайдеры;
   - возврат позиций.

4. В `GameSession` создать мир и построить комнату.

5. Отрисовать:
   - пол;
   - стены;
   - дверь;
   - баннеры;
   - фонтан;
   - ящики;
   - черепа.

## Проверка M1
- комната видна целиком;
- нет прокрутки;
- пол, стены, дверь, фонтан, ящики отображаются;
- нет ошибок загрузки текстур;
- нет 404 в сетевой панели.

---

# M2. Игрок + HUD

## Цель
Добавить передвижение игрока и сердечки.

## Шаги
1. Реализовать в движке:
   - ввод;
   - действия;
   - коллизии;
   - базовую анимацию.

2. Создать фабрику игрока.

3. Добавить компоненты:
   - `PlayerTag`;
   - `PlayerControl`;
   - `Health`;
   - `Invulnerability`;
   - `SpeedBoost`.

4. Создать систему движения игрока.

5. Создать HUD:
   - сердца;
   - монеты;
   - номер волны;
   - баннеры.

6. Временно включить состояние `PLAYING` сразу после старта.

## Проверка M2
- рыцарь ходит по комнате;
- работает `WASD` и стрелки;
- игрок не проходит сквозь стены и ящики;
- анимации `idle`/`run` переключаются;
- спрайт зеркалится;
- сердечки видны;
- нет ошибок.

---

# M3. Оружие и атака

## Цель
Реализовать свинг оружия.

## Шаги
1. Создать данные оружия в `game/data/weapons.ts`.

2. Создать фабрику оружия.

3. Добавить компоненты:
   - `Weapon`;
   - `WeaponAim`;
   - `WeaponSwing`;
   - `AttackCooldown`;
   - `HitboxActive`.

4. Реализовать:
   - поворот оружия к мыши;
   - атаку по ЛКМ/пробелу;
   - дугу свинга;
   - кулдаун;
   - попадание по врагам.

5. Подключить звук-заглушку или реальный `swing`, если звук уже готов.

## Проверка M3
- оружие видно в руке;
- поворачивается за мышью;
- по клику делает взмах;
- кулдаун работает;
- нет ошибок.

---

# M4. Враги

## Цель
Добавить врагов, урон, смерть, дроп.

## Шаги
1. Создать данные врагов.

2. Создать фабрику врагов.

3. Реализовать:
   - обычный ИИ;
   - спавн-эффект;
   - контактный урон;
   - получение урона;
   - вспышку;
   - отброс;
   - смерть;
   - частицы;
   - дроп монет.

4. Реализовать некроманта:
   - держит дистанцию;
   - стреляет.

5. Реализовать снаряды.

6. Реализовать босса:
   - веер снарядов;
   - ярость при низком HP.

7. Временно заспавнить тестовых врагов.

## Проверка M4
- враги бегут к игроку;
- некромант стреляет;
- враги получают урон;
- есть вспышка и отброс;
- враги красиво умирают;
- падают монеты;
- игрок получает урон;
- смерть игрока переходит в `GAME_OVER`.

---

# M5. Пикапы, сундук, волны, босс, доктор

## Цель
Собрать полный игровой цикл.

## Шаги
1. Реализовать пикапы:
   - монета;
   - красная фляга;
   - синяя фляга.

2. Реализовать магнит монет.

3. Реализовать сундук:
   - подсказка `E`;
   - открытие;
   - награды;
   - перезарядка между волнами.

4. Реализовать волновой менеджер:
   - старт волны;
   - спавн по таймеру;
   - отслеживание живых;
   - конец волны;
   - задержка перед следующей.

5. Реализовать доктора:
   - idle;
   - реплики;
   - финальная зона победы.

6. Реализовать дверь:
   - закрыта;
   - открывается после босса.

7. Реализовать последовательность победы.

## Проверка M5
- полная партия играется от волны 1 до босса;
- сундук выдаёт оружие по очереди;
- зелья работают;
- после босса дверь открывается;
- подход к доктору даёт победу;
- смерть даёт поражение.

---

# M6. Меню, пауза, победа, поражение

## Цель
Сделать полноценный мета-цикл.

## Шаги
1. Реализовать главное меню.

2. Реализовать кнопки:
   - Играть;
   - Выход.

3. Реализовать паузу по `Escape`.

4. Реализовать экран поражения.

5. Реализовать экран победы.

6. Реализовать статистику:
   - волны;
   - убийства;
   - монеты;
   - итоговый счёт.

7. Реализовать `restartGame()`:
   - полный сброс мира;
   - пересоздание сцены;
   - без утечек.

## Проверка M6
- игра стартует в меню;
- «Играть» начинает партию;
- `Esc` открывает паузу;
- «Продолжить» возвращает в игру;
- смерть показывает экран поражения;
- «Ещё раз» перезапускает партию;
- победа показывает экран победы;
- 3 рестарта подряд работают без ошибок.

---

# M7. Звук, тряска, шипы, полировка

## Цель
Дать игре “сок”.

## Шаги
1. Реализовать синтез звуков.

2. Подключить все звуки:
   - атака;
   - попадание;
   - урон;
   - смерть;
   - монеты;
   - зелья;
   - сундук;
   - каст;
   - волна;
   - босс;
   - кнопки.

3. Реализовать тряску камеры.

4. Реализовать шипы.

5. Реализовать:
   - искры при попадании;
   - частицы смерти;
   - декал черепа для крупных врагов;
   - вспышку экрана;
   - slow-motion на смерти босса;
   - пульс счётчика монет.

## Проверка M7
- все действия озвучены;
- удары дают тряску и hit-stop;
- шипы работают по циклу;
- шипы ранят и игрока, и врагов;
- смерть босса выглядит эффектно;
- звук не щёлкает бесконечно.

---

# M8. Финальное QA

## Цель
Прогнать полный чек-лист качества.

## Чек-лист
1. Полное прохождение:
   - меню;
   - 5 волн;
   - босс;
   - дверь;
   - доктор;
   - экран победы.

2. Смерть:
   - на волне 1;
   - на боссе;
   - экран поражения;
   - «Ещё раз» работает.

3. Пауза:
   - во время боя;
   - в перерыве между волнами;
   - во время баннера;
   - везде корректно снимается.

4. 3 рестарта подряд:
   - нет утечек;
   - нет ошибок `undefined`/`null`;
   - нет дублирующихся сущностей.

5. Физика:
   - игрок не выходит за стены;
   - не застревает в ящиках;
   - не застревает в сундуке;
   - враги не слипаются в одну точку.

6. Некромант:
   - если упёрся в стену, может стоять и стрелять;
   - не ломает игру.

7. Звук:
   - все звуки слышны;
   - нет бесконечного спама;
   - звук стартует после клика.

8. Ассеты:
   - нет 404;
   - нет выдуманных путей;
   - все пути соответствуют разделу ассетов.

9. Консоль:
   - нет красных ошибок;
   - нет warning про отсутствующие текстуры.

10. Браузер:
   - при потере фокуса игра не падает;
   - после возврата в окно продолжается корректно.

---

# 32. Известные подводные камни

## 32.1. Имена файлов
Нельзя ошибаться:
- `skelet_`, не `skeleton_`;
- `necromancer_anim_f0..f3`, без `idle`/`run`;
- обычный `zombie` не используется;
- `weapon_knife.png` находится в `weapons/`.

## 32.2. Разные размеры спрайтов
Высота спрайтов разная:
- 16;
- 23;
- 28;
- 36.

Обязательно ставить «ноги» в позицию сущности.

## 32.3. Пиксельная графика
- только целые масштабы;
- отключать сглаживание;
- округлять координаты при отрисовке.

## 32.4. Аудио
- `AudioContext` может требовать пользовательского действия;
- не стартовать звук до клика по меню.

## 32.5. Таймеры и пауза
- игровые таймеры должны ставиться на паузу;
- таймеры возврата из hit-stop должны учитывать реальное время.

## 32.6. ECS
- не удалять сущности прямо во время итерации;
- использовать очередь команд;
- не хранить ссылки на удалённые сущности.

## 32.7. Утечки
- очищать слушатели событий;
- удалять DOM-оверлеи;
- не копить партиклы;
- пересоздавать мир при рестарте.

## 32.8. Ввод
- страница не должна скроллиться от стрелок и пробела;
- координаты мыши нужно пересчитывать в логическое разрешение.

## 32.9. Меню в паузе
- кнопки должны быть активны даже если игровая логика остановлена.

## 32.10. Не переусердствовать
Если этап работает:
- не рефакторить бесконечно;
- не добавлять фичи сверх плана;
- лучше рабочая простая игра, чем сломанная сложная.

---

# 33. Соответствие критериям оценки

| Критерий | Чем закрываем |
|---|---|
| Креативность | Защита NPC, барьер, прогрессия оружия, шипы как обоюдоострая тактика |
| Качество реализации | Пошаговые этапы, ECS-структура, разделение движка и игры, QA-чек-лист |
| Геймплей | 5 волн, огр, босс, зелья, кайт некромантов |
| История | Лор в меню, реплики доктора, финальное спасение |
| Полнота | Меню, пауза, победа, поражение, рестарт, статистика |
| Ассеты | 8 типов врагов, 4 оружия, сундук, зелья, монеты, фонтан, баннеры, шипы, дверь, доктор |
| Визуальные эффекты | Вспышки, хит-стоп, тряска, частицы, спавн-фейд, слоу-мо, вспышка экрана |
| Звук | 11 программно синтезированных SFX |
| Архитектура | Движок отдельно, игра отдельно, ECS-подобная модульность |

---

# 34. Порядок работы

Короткая версия:

```text
M0 каркас проекта
M1 комната из ASCII
M2 игрок + HUD
M3 атака
M4 враги
M5 волны / сундук / босс / доктор
M6 меню / пауза / победа / поражение
M7 звук / эффекты / шипы
M8 финальное QA
```

---

# 35. Итоговое требование к реализации

Итоговый проект должен быть:

- **браузерной игрой** на TypeScript/JavaScript;
- с **собственным лёгким движком**;
- с **игровым кодом поверх движка**;
- с **ECS-подобной структурой**;
- с **полным игровым циклом**;
- без внешних аудиофайлов;
- без процедурной генерации;
- без нескольких уровней;
- без лишнего контента сверх плана.

Если реализовать всё по этому плану, получится законченная небольшая игра, максимально близкая к оригинальному Godot-концепту, но уже под **JS/TS-экосистему**.
